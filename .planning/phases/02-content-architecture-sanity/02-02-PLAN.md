---
phase: 02-content-architecture-sanity
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/sanity/schemaTypes/objects/test.ts, src/sanity/schemaTypes/objects/apply.ts, src/sanity/schemaTypes/topic.ts, src/sanity/schemaTypes/index.ts, tests/sanity-fetch.test.ts, tests/schema.test.ts]
autonomous: true
requirements: [CMS-02, CMS-03]

must_haves:
  truths:
    - "Sanity supports Scenario-based and Visual-analysis quiz questions"
    - "Sanity topics include Sandbox configuration metadata for the Apply stage"
    - "A validation test exists that proves Sanity can store and return a 4-stage topic"
  artifacts:
    - path: "src/sanity/schemaTypes/objects/test.ts"
      provides: "Quiz and assessment models"
    - path: "src/sanity/schemaTypes/objects/apply.ts"
      provides: "Project spec and Sandbox configuration models"
    - path: "tests/sanity-fetch.test.ts"
      provides: "Automated validation of the Sanity-to-Client data flow"
---

<objective>
Complete the content architecture by implementing the "Test" (Quiz) and "Apply" (Sandbox) stages and verifying the end-to-end fetching capability. This ensures the CMS can deliver the high-stakes assessment and project-based learning materials required by the gated engine.

Purpose: Enable active evaluation and practical application content modeling.
Output: Quiz and Sandbox schemas + structural validation suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-architecture-sanity/02-RESEARCH.md
@.planning/phases/02-content-architecture-sanity/02-VALIDATION.md
@.planning/phases/02-content-architecture-sanity/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement "Test" Stage (Quizzes)</name>
  <files>src/sanity/schemaTypes/objects/test.ts, src/sanity/schemaTypes/topic.ts, tests/schema.test.ts</files>
  <action>
    Implement assessment models per CMS-03 and update validation:
    1. Create `src/sanity/schemaTypes/objects/test.ts`:
       - Define `quizQuestion` (object):
         - `question` (string)
         - `type` (string, list: ['scenario', 'visual'])
         - `image` (image, hidden if type != 'visual')
         - `options` (array of strings)
         - `correctAnswer` (number, index of options)
         - `explanation` (text)
       - Define `testStage` (object): `questions` (array of `quizQuestion`).
    2. Add `test` field to `topic.ts` using `testStage`.
    3. Update `tests/schema.test.ts` to verify `topic` includes the `test` object and check for `quizQuestion` variants.
  </action>
  <verify>
    <automated>npx vitest tests/schema.test.ts --run</automated>
  </verify>
  <done>Quiz/Test stage implemented with scenario and visual variants.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Apply" Stage (Sandbox Specs)</name>
  <files>src/sanity/schemaTypes/objects/apply.ts, src/sanity/schemaTypes/topic.ts, tests/schema.test.ts</files>
  <action>
    Implement project-based learning models and update validation:
    1. Create `src/sanity/schemaTypes/objects/apply.ts`:
       - Define `sandboxConfig` (object): `runtime` (string), `entryPoint` (string), `requirements` (array of strings).
       - Define `applyStage` (object): `instruction` (text), `spec` (array of `block`), `sandbox` (`sandboxConfig`).
    2. Add `apply` field to `topic.ts` using `applyStage`.
    3. Update `tests/schema.test.ts` to verify `topic` includes the `apply` object and `sandboxConfig` fields.
  </action>
  <verify>
    <automated>npx vitest tests/schema.test.ts --run</automated>
  </verify>
  <done>Apply stage implemented with sandbox metadata and instructions.</done>
</task>

<task type="auto">
  <name>Task 3: Validation & Registration</name>
  <files>src/sanity/schemaTypes/index.ts, tests/sanity-fetch.test.ts</files>
  <action>
    Finalize registration and create a fetch test:
    1. Update `src/sanity/schemaTypes/index.ts` to include the new object types.
    2. Create `tests/sanity-fetch.test.ts`:
       - Use `sanityClient` from `src/lib/sanity.ts`.
       - Write a test that attempts to fetch a topic document and asserts that the `understand`, `reinforce`, `test`, and `apply` objects are present (even if empty).
       - Note: This test can use a mock or a known test document if available, or simply verify the GROQ query structure.
  </action>
  <verify>
    <automated>npx vitest tests/sanity-fetch.test.ts --run</automated>
  </verify>
  <done>New types registered and structural fetch validation passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CMS → Client | Content delivered to the user frontend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | apply.ts | mitigate | Sanity validation to ensure sandbox configuration does not include sensitive internal environment keys or secrets. |
</threat_model>

<verification>
1. Verify `index.ts` exports all types.
2. Run the Vitest schema and fetch tests.
</verification>

<success_criteria>
- Topics support all 4 learning stages (Understand, Reinforce, Test, Apply).
- Quizzes support visual analysis with images.
- Sandbox metadata is structured and fetchable.
- All 4 stages are verified via automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-architecture-sanity/02-02-SUMMARY.md`
</output>
