---
phase: 02-content-architecture-sanity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, sanity.config.ts, src/sanity/schemaTypes/phase.ts, src/sanity/schemaTypes/module.ts, src/sanity/schemaTypes/topic.ts, src/sanity/schemaTypes/index.ts, src/sanity/schemaTypes/chapter.ts, tests/schema.test.ts]
autonomous: true
requirements: [CMS-01, CMS-02]

must_haves:
  truths:
    - "Sanity Studio is configured with @sanity/code-input and sanity-plugin-latex-input"
    - "The 'chapter' schema is removed and replaced by 'topic'"
    - "The 'module' schema is updated to remove JLPT-specific levels"
    - "A 'phase' schema exists to group modules"
  artifacts:
    - path: "src/sanity/schemaTypes/phase.ts"
      provides: "Top-level curriculum grouping"
    - path: "src/sanity/schemaTypes/topic.ts"
      provides: "Learning unit with Understand and Reinforce stages"
    - path: "src/sanity/schemaTypes/objects/understand.ts"
      provides: "Theory and Socratic prompt models"
    - path: "src/sanity/schemaTypes/objects/reinforce.ts"
      provides: "Code and Math practice models"
    - path: "tests/schema.test.ts"
      provides: "Hierarchy and object validation"
  key_links:
    - from: "src/sanity/schemaTypes/topic.ts"
      to: "src/sanity/schemaTypes/module.ts"
      via: "reference field"
    - from: "src/sanity/schemaTypes/module.ts"
      to: "src/sanity/schemaTypes/phase.ts"
      via: "reference field"
---

<objective>
Initialize the ML-centric Sanity schema hierarchy and implement the foundation for the "Understand" and "Reinforce" learning stages. This plan transitions the CMS from the old JLPT structure to the new 3-tier ML curriculum (Phase -> Module -> Topic).

Purpose: Establish the structural backbone for ML content delivery.
Output: Hierarchical schemas and active learning content blocks (Socratic, Code, Math).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-architecture-sanity/02-RESEARCH.md
@.planning/phases/02-content-architecture-sanity/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Install Sanity Plugins</name>
  <files>package.json, sanity.config.ts</files>
  <action>
    Install and configure the necessary Sanity plugins for ML content:
    1. Run `npm install @sanity/code-input sanity-plugin-latex-input`
    2. Update `sanity.config.ts` to import and include `codeInput()` and `latexInput()` in the `plugins` array.
    3. Update the `title` in `defineConfig` to "ML Cognitive Coach".
  </action>
  <verify>
    <automated>npm list @sanity/code-input sanity-plugin-latex-input</automated>
  </verify>
  <done>Plugins installed and registered in sanity.config.ts</done>
</task>

<task type="auto">
  <name>Task 1: Implement Core Hierarchy (Phase, Module, Topic) & Validation Scaffold</name>
  <files>src/sanity/schemaTypes/phase.ts, src/sanity/schemaTypes/module.ts, src/sanity/schemaTypes/topic.ts, tests/schema.test.ts</files>
  <action>
    Implement the 3-tier hierarchy per RESEARCH.md and establish the validation suite:
    1. Create `phase.ts`: Fields: `title` (string), `slug` (slug), `order` (number), `description` (text).
    2. Update `module.ts`: 
       - Remove JLPT `level` field.
       - Add `phase` (reference to 'phase').
       - Keep `title`, `description`, `order`.
    3. Create `topic.ts` (Skeleton):
       - Fields: `title` (string), `slug` (slug, source: title), `module` (reference to 'module'), `order` (number).
       - Add placeholders for stages: `understand` (object), `reinforce` (object).
    4. Create `tests/schema.test.ts`:
       - Write tests using Vitest to import these schema files.
       - Assert that `phase` exists and has `name: 'phase'`.
       - Assert that `module` has a reference to `phase`.
       - Assert that `topic` has a reference to `module`.
  </action>
  <verify>
    <automated>npx vitest tests/schema.test.ts --run</automated>
  </verify>
  <done>Phase, Module (updated), and Topic schemas defined and validated via Vitest.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Understand" & "Reinforce" Stages</name>
  <files>src/sanity/schemaTypes/objects/understand.ts, src/sanity/schemaTypes/objects/reinforce.ts, src/sanity/schemaTypes/topic.ts, tests/schema.test.ts</files>
  <action>
    Implement the interactive content models and update validation:
    1. Create `src/sanity/schemaTypes/objects/understand.ts`:
       - Define `socraticPrompt` (object): `question` (string), `hint` (text), `explanation` (text).
       - Define `understand` (object): `content` (array of `block` and `socraticPrompt`).
    2. Create `src/sanity/schemaTypes/objects/reinforce.ts`:
       - Define `reinforce` (object): `practices` (array of `code` [from plugin] and `latex` [from plugin]).
       - Ensure `code` block uses `options: { language: 'python' }` as default.
    3. Wire these objects into `topic.ts`.
    4. Update `tests/schema.test.ts` to verify `topic` includes `understand` and `reinforce` objects.
  </action>
  <verify>
    <automated>npx vitest tests/schema.test.ts --run</automated>
  </verify>
  <done>Understand and Reinforce stage models implemented with Socratic, Code, and Math support.</done>
</task>

<task type="auto">
  <name>Task 3: Schema Cleanup & Registration</name>
  <files>src/sanity/schemaTypes/index.ts, src/sanity/schemaTypes/chapter.ts</files>
  <action>
    Clean up the legacy JLPT schemas:
    1. Delete `src/sanity/schemaTypes/chapter.ts`.
    2. Update `src/sanity/schemaTypes/index.ts` to export: `phase`, `module`, `topic`, and any necessary object types.
  </action>
  <verify>
    <automated>test ! -f src/sanity/schemaTypes/chapter.ts && grep "topic" src/sanity/schemaTypes/index.ts</automated>
  </verify>
  <done>Legacy chapter schema removed and new types registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Editor → CMS | Authors provide the learning content and metadata |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | topic.ts | mitigate | Use Sanity validation rules to ensure slugs are unique and required to prevent orphaned content in Prisma. |
</threat_model>

<verification>
1. Run `npx vitest tests/schema.test.ts` to confirm hierarchy and stage structure.
2. Run `npx sanity schema validate` (if available in current CLI) to check for Sanity-specific errors.
</verification>

<success_criteria>
- Phase, Module, and Topic types appear in Sanity Studio.
- Topics can be created with Socratic prompts and Python code blocks.
- Legacy JLPT Chapter type is gone.
- Hierarchy validation test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-architecture-sanity/02-01-SUMMARY.md`
</output>
