---
phase: 02-progress-persistence
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/components/ChapterCompleteButton.tsx, src/app/modules/[id]/page.tsx]
autonomous: true
requirements: [PROG-02, PROG-03]

must_haves:
  truths:
    - "Chapter completion button reflects persistent database state"
    - "Module Detail page shows progress indicators for each chapter"
    - "No authentication is required for progress tracking"
  artifacts:
    - path: "src/components/ChapterCompleteButton.tsx"
      provides: "Client-side progress toggling"
    - path: "src/app/modules/[id]/page.tsx"
      provides: "Server-side progress visualization on module page"
  key_links:
    - from: "src/components/ChapterCompleteButton.tsx"
      to: "/api/progress/[chapterId]"
      via: "fetch API"
---

<objective>
Integrate persistent progress tracking into the user interface.

Purpose: Provide visual feedback to users about their learning progress across chapters and modules.
Output: Updated Chapter button and Module Detail page with progress indicators.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/components/ChapterCompleteButton.tsx
@src/app/modules/[id]/page.tsx
@src/app/api/progress/[chapterId]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ChapterCompleteButton Component</name>
  <files>src/components/ChapterCompleteButton.tsx</files>
  <action>
    Modify `src/components/ChapterCompleteButton.tsx` to use the new database-driven API.
    - Remove `useSession` and `next-auth` imports.
    - Use `useEffect` to fetch the initial progress state from `GET /api/progress/[chapterId]` on mount.
    - Update `handleToggleComplete` to call `POST /api/progress/[chapterId]`.
    - Remove `moduleId` requirement if it's no longer used in the simplified schema (the user requested just `chapterId` and `completedAt`).
    - Remove alert and restriction for unauthenticated users.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Button component correctly fetches and toggles progress state without authentication.</done>
</task>

<task type="auto">
  <name>Task 2: Add Progress Indicators to Module Detail Page</name>
  <files>src/app/modules/[id]/page.tsx</files>
  <action>
    Enhance `src/app/modules/[id]/page.tsx` to show which chapters are completed.
    - Import `prisma` from `@/lib/prisma`.
    - Fetch all completed chapter IDs from the database: `prisma.progress.findMany({ select: { chapterId: true } })`.
    - Map over the chapters in the module and check if their ID (from Sanity) exists in the progress records.
    - Render a visual indicator (e.g., a green "✓" or "Completed" badge) next to each completed chapter in the list.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Module detail page displays completion status for each chapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Database | Server Component queries Prisma directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Spoofing | Client UI | accept | Progress is global/public; no sensitive data at risk |
</threat_model>

<verification>
1. Navigate to a chapter page and click "Mark Completed".
2. Refresh the page and ensure the button still says "Completed".
3. Navigate to the parent module page and verify a checkmark appears next to that chapter.
4. Click "Completed" again on the chapter page to unmark it, and verify it updates on the module page.
</verification>

<success_criteria>
- User can toggle chapter completion with persistence.
- Module page accurately reflects completion status from the database.
- Entire flow works without login or session requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-progress-persistence/02-02-SUMMARY.md`
</output>
