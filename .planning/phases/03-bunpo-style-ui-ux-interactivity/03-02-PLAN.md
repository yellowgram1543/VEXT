---
phase: 03-bunpo-style-ui-ux-interactivity
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/app/chapters/[slug]/page.tsx, src/components/GrammarCard.tsx, src/components/InteractiveExample.tsx]
autonomous: false
requirements: [UI-02, UI-05]
must_haves:
  truths:
    - "Grammar content is presented in card containers with subtle shadows"
    - "Example sentences have a 'Play Audio' icon placeholder"
    - "Example sentences allow toggling translation/romaji visibility"
    - "PortableText renderer correctly maps blocks to these new components"
  artifacts:
    - path: "src/components/GrammarCard.tsx"
      provides: "Card container for lesson sections"
    - path: "src/components/InteractiveExample.tsx"
      provides: "Interactive sentence component with toggles"
    - path: "src/app/chapters/[slug]/page.tsx"
      provides: "Updated PortableText renderer"
  key_links:
    - from: "src/app/chapters/[slug]/page.tsx"
      to: "src/components/GrammarCard.tsx"
      via: "import in portableTextComponents"
    - from: "src/app/chapters/[slug]/page.tsx"
      to: "src/components/InteractiveExample.tsx"
      via: "import in portableTextComponents"
---

<objective>
Redesign the lesson content into an interactive, card-based experience. This replicates the focused, bite-sized learning feel of Bunpo.

Purpose: Focused learning through immersive UI components.
Output: New GrammarCard and InteractiveExample components integrated into the chapter pages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/chapters/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI Components for Lessons</name>
  <files>src/components/GrammarCard.tsx, src/components/InteractiveExample.tsx</files>
  <action>
    - Create `src/components/GrammarCard.tsx`: A wrapper component that renders children inside a premium-style card (white bg, rounded-3xl, subtle border/shadow).
    - Create `src/components/InteractiveExample.tsx`: 
      - Accepts `japanese`, `romaji`, `english`.
      - Features a "Play" icon (Lucide Play or similar) as a placeholder for future audio.
      - Implements click-to-reveal or toggle buttons for `romaji` and `english`.
      - Uses large, clear typography for Japanese text.
    - Ensure both are fully responsive (mobile-first).
  </action>
  <verify>
    <automated>ls src/components/GrammarCard.tsx src/components/InteractiveExample.tsx</automated>
  </verify>
  <done>New interactive components created with Bunpo-style aesthetics.</done>
</task>

<task type="auto">
  <name>Task 2: Update PortableText Renderer</name>
  <files>src/app/chapters/[slug]/page.tsx</files>
  <action>
    - Import `GrammarCard` and `InteractiveExample`.
    - Update `portableTextComponents`:
      - Map `normal` blocks and `h1-h3` headers to be wrapped in `GrammarCard` or styled consistently within them.
      - Update `exampleSentence` custom type to use the new `InteractiveExample` component.
    - Adjust margins and padding to ensure cards have enough whitespace between them.
  </action>
  <verify>
    <automated>grep -q "InteractiveExample" src/app/chapters/[slug]/page.tsx</automated>
  </verify>
  <done>Chapter page uses new card-based rendering for all content types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive Grammar Cards</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Visit a chapter page (e.g., `/chapters/grammar-intro`).
    3. Verify that content is organized into cards.
    4. Click an example sentence to test translation/romaji toggles.
    5. Verify the layout looks clean on mobile screen sizes.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Untrusted content from Sanity is rendered via PortableText.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01| I | PortableText | mitigate | Use `@portabletext/react` which handles sanitization of HTML by default. |
</threat_model>

<verification>
Ensure the "card" feel is prominent and each section feels focused.
</verification>

<success_criteria>
- Content is in cards.
- Interactive sentences work as expected.
- Design is responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bunpo-style-ui-ux-interactivity/03-02-SUMMARY.md`
</output>
