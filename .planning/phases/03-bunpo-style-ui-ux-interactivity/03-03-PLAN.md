---
phase: 03-bunpo-style-ui-ux-interactivity
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/app/page.tsx, src/app/modules/[id]/page.tsx, src/components/ProgressBar.tsx, src/components/ChapterCompleteButton.tsx]
autonomous: false
requirements: [UI-03, UI-05]
must_haves:
  truths:
    - "Progress bars are visible on the Home and Module pages"
    - "Progress calculation uses real data from the progress API/store"
    - "Chapter completion button has a smooth micro-interaction upon click"
    - "Page transitions between chapters feel smooth"
  artifacts:
    - path: "src/components/ProgressBar.tsx"
      provides: "Reusable circular/linear progress visualization"
    - path: "src/app/page.tsx"
      provides: "Module listing with progress"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ProgressBar.tsx"
      via: "import"
---

<objective>
Visualize user progress and add final interaction polish to make the app feel alive and rewarding. This completes the Bunpo-style transformation.

Purpose: Feedback loops and gamification.
Output: Progress bars and polished interactions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/app/modules/[id]/page.tsx
@src/components/ChapterCompleteButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Progress Visualization</name>
  <files>src/components/ProgressBar.tsx, src/app/page.tsx, src/app/modules/[id]/page.tsx</files>
  <action>
    - Create `src/components/ProgressBar.tsx`: A component that renders a sleek, thin progress bar (linear or circular) given a percentage.
    - Integrate `ProgressBar` into the Home page (`src/app/page.tsx`): Show completion percentage for each module.
    - Integrate `ProgressBar` into the Module Detail page: Show overall module progress at the top.
    - Note: Use the data fetching patterns established in Phase 2 to get completion counts.
  </action>
  <verify>
    <automated>ls src/components/ProgressBar.tsx</automated>
  </verify>
  <done>Progress bars are integrated into Home and Module pages.</done>
</task>

<task type="auto">
  <name>Task 2: Add Interaction Polish</name>
  <files>src/components/ChapterCompleteButton.tsx, src/app/globals.css</files>
  <action>
    - Update `ChapterCompleteButton.tsx`: Add a "success" state with a checkmark and a slight bounce or scale-up animation using CSS transitions or Framer Motion (if available, otherwise standard CSS).
    - Add smooth page transitions in `globals.css` using View Transitions API if supported or simple fade-in classes for main content.
    - Ensure all buttons have consistent, soft hover states matching the "premium" palette.
  </action>
  <verify>
    <automated>grep -q "transition" src/components/ChapterCompleteButton.tsx</automated>
  </verify>
  <done>Micro-interactions and transitions are applied to key learning actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Progress & Interaction Polish</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Check the Home page for module progress bars.
    3. Complete a chapter and verify the button animation.
    4. Verify the progress bar updates (if persistence is working).
    5. Navigate between chapters and check for smooth feel.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Progress data retrieved from API.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01| I | ProgressBar | accept | Purely visual component, no input. |
</threat_model>

<verification>
Check that the "Complete" button feel is satisfying and progress is clear.
</verification>

<success_criteria>
- Progress is visualized on modules.
- Button interactions are smooth.
- App feels "polished".
</success_criteria>

<output>
After completion, create `.planning/phases/03-bunpo-style-ui-ux-interactivity/03-03-SUMMARY.md`
</output>
