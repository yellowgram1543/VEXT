---
phase: 03-bunpo-style-ui-ux-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/globals.css, src/app/layout.tsx]
autonomous: false
requirements: [UI-01, UI-05]
must_haves:
  truths:
    - "Typography uses Geist Sans or Inter for a modern look"
    - "Color palette uses soft whites, light blues, and subtle grays"
    - "Consistent spacing is applied globally via CSS variables"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Global styles and Tailwind theme"
    - path: "src/app/layout.tsx"
      provides: "Font configuration and base layout"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import"
---

<objective>
Refine global aesthetics to achieve a high-end, "premium" feel inspired by the Bunpo app. This establishes the foundation for a minimalist, clean UI.

Purpose: Consistency in typography, color, and spacing.
Output: Updated global CSS and layout with modern fonts and soft palette.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Global Typography and Theme</name>
  <files>src/app/globals.css</files>
  <action>
    - Update `@theme inline` in `src/app/globals.css` to use a soft color palette.
    - Define variables for:
      - `--background`: #F8FAFC (very light blue/gray white)
      - `--card-bg`: #FFFFFF
      - `--accent-blue`: #3B82F6
      - `--text-main`: #1E293B
      - `--text-soft`: #64748B
    - Ensure Geist Sans (or Inter) is the primary sans-serif font.
    - Add utility classes for "soft shadow" and "premium card" feel if needed.
  </action>
  <verify>
    <automated>grep -q "var(--background)" src/app/globals.css</automated>
  </verify>
  <done>Global CSS updated with soft palette and Geist/Inter font variables.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Layout and Fonts</name>
  <files>src/app/layout.tsx</files>
  <action>
    - Ensure Geist Sans and Geist Mono are correctly configured using `next/font/local` or `next/font/google`.
    - Apply the font variables to the `body` tag.
    - Set base background color to the new soft background variable.
    - Remove any default Arial/Helvetica fallbacks that clash with the "premium" look.
  </action>
  <verify>
    <automated>grep -q "Geist" src/app/layout.tsx</automated>
  </verify>
  <done>layout.tsx uses Geist font and applies global theme classes to the body.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Global aesthetics foundation (Typography & Palette)</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Visit the home page.
    3. Verify the background is a soft off-white/light-blue.
    4. Verify the font is clean and modern (Geist Sans).
    5. Check that spacing feels consistent and "airy" (minimalist).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- None directly impacted by CSS/Typography changes.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01| S | globals.css | accept | Static CSS file, low risk. |
</threat_model>

<verification>
Check that the site doesn't feel like a standard "Tailwind template" but has a custom, soft, Bunpo-like aesthetic.
</verification>

<success_criteria>
- Global background is soft white/blue.
- Typography is Inter/Geist.
- Site feels minimalist and premium.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bunpo-style-ui-ux-interactivity/03-01-SUMMARY.md`
</output>
