---
phase: 04-execution-engine-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/lib/evaluators/math.ts, src/lib/evaluators/python.ts, tests/evaluators.test.ts]
autonomous: true
requirements: [EXEC-01]

must_haves:
  truths:
    - "Math expressions can be compared for symbolic equivalence"
    - "Python code can be executed and its output captured"
    - "Evaluators are tested and handle basic errors/timeouts"
  artifacts:
    - path: "src/lib/evaluators/math.ts"
      provides: "Math expression comparison logic"
    - path: "src/lib/evaluators/python.ts"
      provides: "Python code execution wrapper"
    - path: "tests/evaluators.test.ts"
      provides: "Validation for evaluation logic"
  key_links:
    - from: "src/lib/evaluators/python.ts"
      to: "child_process"
      via: "execSync or spawn"
---

<objective>
Establish the backend foundation for evaluating user submissions.

Purpose: To provide reliable utilities for checking Python code output and mathematical expressions.
Output: Evaluation utilities and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-engine-integration/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Math Evaluator</name>
  <files>package.json, src/lib/evaluators/math.ts</files>
  <action>
    1. Install `mathjs` using `npm install mathjs`.
    2. Create `src/lib/evaluators/math.ts`.
    3. Implement `compareMath(input: string, expected: string): boolean` using `mathjs.simplify` or `mathjs.evaluate` for symbolic/numeric comparison.
    4. Handle basic edge cases (whitespace, equivalent forms like 0.5 vs 1/2).
  </action>
  <verify>
    <automated>ls src/lib/evaluators/math.ts</automated>
  </verify>
  <done>Math evaluator implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Python Evaluator</name>
  <files>src/lib/evaluators/python.ts</files>
  <action>
    1. Create `src/lib/evaluators/python.ts`.
    2. Implement `runPython(code: string, timeoutMs: number = 5000): Promise<{ stdout: string, stderr: string, error?: string }>`.
    3. Use `child_process.exec` to run the code.
    4. Implement basic security: wrap the user code in a try-except and limit execution time.
    5. Note: Assume `python` or `python3` is available in the environment.
  </action>
  <verify>
    <automated>ls src/lib/evaluators/python.ts</automated>
  </verify>
  <done>Python evaluator implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 3: Validate Evaluators with Tests</name>
  <files>tests/evaluators.test.ts</files>
  <action>
    1. Create `tests/evaluators.test.ts`.
    2. Add test cases for `compareMath`:
       - "x + y" vs "y + x" -> true
       - "2 * 3" vs "6" -> true
       - "0.5" vs "1/2" -> true
    3. Add test cases for `runPython`:
       - `print("hello")` -> stdout: "hello\n"
       - `print(1 + 1)` -> stdout: "2\n"
       - Infinite loop -> should timeout.
  </action>
  <verify>
    <automated>npx vitest tests/evaluators.test.ts --run</automated>
  </verify>
  <done>Evaluators pass all unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | User submitted code/math enters the system. |
| API -> Shell | Python code is executed in a sub-process. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Python Evaluator | Mitigate | Wrap code in boilerplate, set strict timeout, and avoid importing dangerous modules (limited mitigation for prototype). |
| T-04-02 | Denial of Service | Python Evaluator | Mitigate | Enforce 5s timeout on sub-process execution. |
</threat_model>

<success_criteria>
1. `mathjs` is installed and functioning.
2. `compareMath` correctly identifies equivalent mathematical expressions.
3. `runPython` executes code and returns results or timeout errors.
4. All tests in `tests/evaluators.test.ts` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-engine-integration/04-01-SUMMARY.md`
</output>
