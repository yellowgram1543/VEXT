---
phase: 04-execution-engine-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, prisma/schema.prisma, src/lib/evaluators/math.ts, src/lib/evaluators/python.ts, tests/evaluators.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "Math expressions can be compared for symbolic equivalence"
    - "Python code can be executed and its output captured"
    - "Database schema supports recording practice and apply submissions"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Submission model for history tracking"
    - path: "src/lib/evaluators/math.ts"
      provides: "Math expression comparison logic"
    - path: "src/lib/evaluators/python.ts"
      provides: "Python code execution wrapper"
---

<objective>
Establish the backend foundation for evaluating user submissions and recording results.

Purpose: To provide reliable utilities for checking Python code output and mathematical expressions, and the persistence layer for submission history.
Output: Evaluation utilities, Prisma schema updates, and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-engine-integration/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma Schema for Submissions</name>
  <files>prisma/schema.prisma</files>
  <action>
    1. Add a `Submission` model to `prisma/schema.prisma` per EXEC-03:
       ```prisma
       model Submission {
         id         String    @id @default(cuid())
         topicId    String
         stage      StageType
         userId     String?   // Optional for prototype
         content    String    // The submitted code or math
         result     String?   // The output or result of evaluation
         success    Boolean
         createdAt  DateTime  @default(now())
       }
       ```
    2. Run `npx prisma generate`.
    3. Note: Since we are in dev/prototype, we can use `npx prisma db push` during execution.
  </action>
  <verify>
    <automated>grep "model Submission" prisma/schema.prisma</automated>
  </verify>
  <done>Submission model added to schema and client generated.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Math Evaluator</name>
  <files>package.json, src/lib/evaluators/math.ts</files>
  <action>
    1. Install `mathjs` per EXEC-01.
    2. Create `src/lib/evaluators/math.ts`.
    3. Implement `compareMath(input: string, expected: string): boolean` using `mathjs.simplify` for symbolic comparison.
    4. Handle basic edge cases (whitespace, equivalent forms).
  </action>
  <verify>
    <automated>ls src/lib/evaluators/math.ts</automated>
  </verify>
  <done>Math evaluator implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 3: Setup Python Evaluator</name>
  <files>src/lib/evaluators/python.ts</files>
  <action>
    1. Create `src/lib/evaluators/python.ts` per EXEC-01.
    2. Implement `runPython(code: string, timeoutMs: number = 5000): Promise<{ stdout: string, stderr: string, error?: string }>`.
    3. Use `child_process.exec` to run the code.
    4. Implement basic security: wrap the user code in a try-except and limit execution time.
  </action>
  <verify>
    <automated>ls src/lib/evaluators/python.ts</automated>
  </verify>
  <done>Python evaluator implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 4: Validate Evaluators with Tests</name>
  <files>tests/evaluators.test.ts</files>
  <action>
    1. Create `tests/evaluators.test.ts`.
    2. Add test cases for `compareMath` (e.g., symbolic equality).
    3. Add test cases for `runPython` (e.g., output capture, timeout).
  </action>
  <verify>
    <automated>npx vitest tests/evaluators.test.ts --run</automated>
  </verify>
  <done>Evaluators pass all unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | User submitted code/math enters the system. |
| API -> Shell | Python code is executed in a sub-process. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Python Evaluator | Mitigate | Set strict timeout, and avoid importing dangerous modules (limited mitigation for prototype). |
| T-04-02 | Denial of Service | Python Evaluator | Mitigate | Enforce 5s timeout on sub-process execution. |
</threat_model>

<success_criteria>
1. `mathjs` is installed and functioning.
2. `Submission` model exists in the database.
3. `compareMath` correctly identifies equivalent mathematical expressions.
4. `runPython` executes code and returns results or timeout errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-engine-integration/04-01-SUMMARY.md`
</output>
