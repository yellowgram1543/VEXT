---
phase: 04-execution-engine-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/lib/learning-engine.ts, src/app/api/evaluate/practice/route.ts, src/app/api/evaluate/quiz/route.ts, src/app/api/evaluate/apply/route.ts, tests/engine-integration.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "Practice submissions trigger stage unlocking and are recorded in DB"
    - "Quiz submissions record attempts and enforce mastery gate"
    - "Apply submissions are evaluated, recorded, and mark topic as completed"
  artifacts:
    - path: "src/app/api/evaluate/practice/route.ts"
      provides: "Practice evaluation endpoint"
    - path: "src/app/api/evaluate/quiz/route.ts"
      provides: "Quiz recording and progression endpoint"
    - path: "src/app/api/evaluate/apply/route.ts"
      provides: "Final project evaluation endpoint"
    - path: "tests/engine-integration.test.ts"
      provides: "E2E testing for the state machine"
---

<objective>
Integrate the evaluation engines with the state machine, record history, and expose them via API.

Purpose: To bridge the frontend interactions with backend persistence, recording every submission for progress tracking and history.
Output: API routes, updated learning engine, and integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-engine-integration/04-01-SUMMARY.md
@src/lib/learning-engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Learning Engine with Recording</name>
  <files>src/lib/learning-engine.ts</files>
  <action>
    1. Update `evaluatePractice(topicId: string, type: 'math' | 'code', submission: string, expected: string)`:
       - Use evaluators from 04-01.
       - Create a `Submission` record in Prisma (EXEC-03).
       - If successful, call `unlockNextStage(topicId, StageType.PRACTICE)` per EXEC-02.
    2. Update `evaluateApply(topicId: string, submission: string)`:
       - Run code and validate.
       - Create a `Submission` record in Prisma (EXEC-03).
       - If successful, update progress to mark topic as completed.
    3. Update `submitQuiz`:
       - Ensure it records `QuizAttempt` and returns results.
  </action>
  <verify>
    <automated>grep "prisma.submission.create" src/lib/learning-engine.ts</automated>
  </verify>
  <done>Learning engine updated with evaluation and recording logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Practice and Quiz Endpoints</name>
  <files>src/app/api/evaluate/practice/route.ts, src/app/api/evaluate/quiz/route.ts</files>
  <action>
    1. Create `src/app/api/evaluate/practice/route.ts`:
       - Accept POST { topicId, type, submission, expected }.
       - Call `learningEngine.evaluatePractice`.
       - Return { success, result, nextStage }.
    2. Create `src/app/api/evaluate/quiz/route.ts`:
       - Accept POST { topicId, score, passed }.
       - Call `learningEngine.submitQuiz`.
       - Return { passed, score, nextStage }.
  </action>
  <verify>
    <automated>ls src/app/api/evaluate/practice/route.ts src/app/api/evaluate/quiz/route.ts</automated>
  </verify>
  <done>API endpoints for practice and quiz implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Apply Endpoint</name>
  <files>src/app/api/evaluate/apply/route.ts</files>
  <action>
    1. Create `src/app/api/evaluate/apply/route.ts`:
       - Accept POST { topicId, submission }.
       - Call `learningEngine.evaluateApply`.
       - Return { success, result, completed: true }.
  </action>
  <verify>
    <automated>ls src/app/api/evaluate/apply/route.ts</automated>
  </verify>
  <done>API endpoint for apply stage implemented.</done>
</task>

<task type="auto">
  <name>Task 4: State Machine Integration Tests</name>
  <files>tests/engine-integration.test.ts</files>
  <action>
    1. Create `tests/engine-integration.test.ts`.
    2. Add tests that simulate a user moving through PRACTICE -> TEST -> APPLY.
    3. Verify that database state (Progress, Submission, QuizAttempt) matches expectations at each step.
  </action>
  <verify>
    <automated>npx vitest tests/engine-integration.test.ts --run</automated>
  </verify>
  <done>Integration tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | Evaluation results are persisted to Prisma. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Evaluation API | Mitigate | Return sanitized feedback; log detailed errors only on the server. |
</threat_model>

<success_criteria>
1. All API endpoints are reachable and return expected structures.
2. `learning-engine.ts` correctly handles state transitions and records history.
3. Submissions are successfully stored in the `Submission` table.
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-engine-integration/04-02-SUMMARY.md`
</output>
