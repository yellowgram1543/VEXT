---
phase: 04-execution-engine-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/lib/learning-engine.ts, src/app/api/evaluate/practice/route.ts, src/app/api/evaluate/quiz/route.ts, src/app/api/evaluate/apply/route.ts]
autonomous: true
requirements: [EXEC-02]

must_haves:
  truths:
    - "Practice submissions trigger stage unlocking (PRACTICE -> TEST)"
    - "Quiz submissions record attempts and enforce mastery gate"
    - "Apply submissions are evaluated and mark topic as completed"
  artifacts:
    - path: "src/app/api/evaluate/practice/route.ts"
      provides: "Practice evaluation endpoint"
    - path: "src/app/api/evaluate/quiz/route.ts"
      provides: "Quiz recording and progression endpoint"
    - path: "src/app/api/evaluate/apply/route.ts"
      provides: "Final project evaluation endpoint"
  key_links:
    - from: "src/app/api/evaluate/*"
      to: "src/lib/learning-engine.ts"
      via: "Function calls"
---

<objective>
Integrate the evaluation engines with the state machine and expose them via API.

Purpose: To bridge the frontend interactions with the backend persistence and progression logic.
Output: API routes and updated learning engine.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-engine-integration/04-01-SUMMARY.md
@src/lib/learning-engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Learning Engine</name>
  <files>src/lib/learning-engine.ts</files>
  <action>
    1. Implement `evaluatePractice(topicId: string, type: 'math' | 'code', submission: string, expected: string)`:
       - Use evaluators from 04-01.
       - If successful, call `unlockNextStage(topicId, StageType.PRACTICE)` to move to `TEST`.
    2. Implement `evaluateApply(topicId: string, submission: string)`:
       - Run code and validate.
       - If successful, update progress to `highestStage: APPLY` and set `completedAt`.
    3. Refactor `submitQuiz` to ensure it returns the result (passed/score) for the API to consume.
  </action>
  <verify>
    <automated>grep "evaluatePractice" src/lib/learning-engine.ts</automated>
  </verify>
  <done>Learning engine updated with evaluation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Practice and Quiz Endpoints</name>
  <files>src/app/api/evaluate/practice/route.ts, src/app/api/evaluate/quiz/route.ts</files>
  <action>
    1. Create `src/app/api/evaluate/practice/route.ts`:
       - Accept POST { topicId, type, submission, expected }.
       - Call `learningEngine.evaluatePractice`.
       - Return { success, message, nextStage }.
    2. Create `src/app/api/evaluate/quiz/route.ts`:
       - Accept POST { topicId, userId, score }.
       - Call `learningEngine.submitQuiz`.
       - Return { passed, score, nextStage: APPLY if passed }.
  </action>
  <verify>
    <automated>ls src/app/api/evaluate/practice/route.ts src/app/api/evaluate/quiz/route.ts</automated>
  </verify>
  <done>API endpoints for practice and quiz implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Apply Endpoint</name>
  <files>src/app/api/evaluate/apply/route.ts</files>
  <action>
    1. Create `src/app/api/evaluate/apply/route.ts`:
       - Accept POST { topicId, submission }.
       - Call `learningEngine.evaluateApply`.
       - Return { success, completed: true }.
  </action>
  <verify>
    <automated>ls src/app/api/evaluate/apply/route.ts</automated>
  </verify>
  <done>API endpoint for apply stage implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | Evaluation results are persisted to Prisma. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Evaluation API | Mitigate | Do not return detailed system error messages to the client; return sanitized feedback. |
</threat_model>

<success_criteria>
1. All API endpoints are reachable and return expected structures.
2. `learning-engine.ts` correctly handles the state transitions based on evaluation results.
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-engine-integration/04-02-SUMMARY.md`
</output>
