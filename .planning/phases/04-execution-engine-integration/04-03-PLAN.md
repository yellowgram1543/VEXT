---
phase: 04-execution-engine-integration
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [src/components/stages/PracticeCell.tsx, src/components/stages/QuizCell.tsx, src/components/stages/ApplyCell.tsx, src/components/TopicFlow.tsx]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "User can submit code/math in Practice stage and see real-time feedback"
    - "Quiz completion triggers state-machine transitions and UI unlocking"
    - "Final 'Apply' stage allows code submission and topic completion"
  artifacts:
    - path: "src/components/stages/ApplyCell.tsx"
      provides: "Final laboratory project UI"
---

<objective>
Connect the UI components to the evaluation APIs and finalize the gated learning flow.

Purpose: To provide the end-user with an interactive, feedback-driven learning experience that bridges UI interactions with backend evaluation.
Output: Integrated UI components and a fully functional gated learning flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-engine-integration/04-02-SUMMARY.md
@src/components/stages/PracticeCell.tsx
@src/components/stages/QuizCell.tsx
@src/components/TopicFlow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Practice and Quiz Cells</name>
  <files>src/components/stages/PracticeCell.tsx, src/components/stages/QuizCell.tsx</files>
  <action>
    1. Update `PracticeCell.tsx`:
       - Implement `handleSubmit` to call `/api/evaluate/practice`.
       - Show success/error toasts using the existing UI patterns.
       - Use `router.refresh()` on success to trigger server-side state re-fetch.
    2. Update `QuizCell.tsx`:
       - Connect the final submission step to call `/api/evaluate/quiz`.
       - Record `passed` status based on the score (>= 80%).
       - On success, show celebratory UI and `router.refresh()`.
  </action>
  <verify>
    <automated>grep "fetch" src/components/stages/PracticeCell.tsx src/components/stages/QuizCell.tsx</automated>
  </verify>
  <done>Practice and Quiz cells are connected to evaluation APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement and Integrate Apply Cell</name>
  <files>src/components/stages/ApplyCell.tsx, src/components/TopicFlow.tsx</files>
  <action>
    1. Create `src/components/stages/ApplyCell.tsx`:
       - Implementation of a Project Laboratory UI (text area for code, submit button).
       - Connection to `/api/evaluate/apply`.
       - Success state showing "Topic Mastered" badge.
    2. Update `TopicFlow.tsx`:
       - Replace the placeholder in the Apply Block with the `<ApplyCell />`.
       - Ensure `TopicFlow` correctly handles the gated visibility of the Apply stage.
  </action>
  <verify>
    <automated>grep "ApplyCell" src/components/TopicFlow.tsx</automated>
  </verify>
  <done>Apply stage is implemented and integrated into the topic flow.</done>
</task>

<task type="auto">
  <name>Task 3: Comprehensive Flow Verification</name>
  <files>src/components/TopicFlow.tsx</files>
  <action>
    1. Perform a complete walkthrough of a single Topic:
       - Verify `PRACTICE` unlocks `TEST` upon correct math/code submission.
       - Verify `TEST` (Quiz) unlocks `APPLY` only when score >= 80%.
       - Verify `APPLY` (Project) marks the topic as `COMPLETED` in the dashboard.
    2. Check the database (`npx prisma studio`) to ensure:
       - `Submission` records exist for Practice and Apply stages.
       - `QuizAttempt` records exist for Test stage.
       - `Progress.completedAt` is set after Apply stage completion.
    3. Document findings and any edge case handling in the phase summary.
  </action>
  <verify>
    <automated>ls src/components/stages/ApplyCell.tsx</automated>
  </verify>
  <done>Gated flow is fully verified, from submission to persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> API | User input is sent for evaluation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | Quiz Submission | Accept | Prototype assumes trusted client for stage unlocking. |
</threat_model>

<success_criteria>
1. Users can progress through all 5 stages of a topic.
2. Code and math are evaluated in real-time with UI feedback.
3. Database correctly reflects the user's progress and submission history.
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-engine-integration/04-03-SUMMARY.md`
</output>
