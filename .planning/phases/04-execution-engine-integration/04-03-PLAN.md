---
phase: 04-execution-engine-integration
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [src/components/stages/PracticeCell.tsx, src/components/stages/QuizCell.tsx, src/components/stages/ApplyCell.tsx, src/components/TopicFlow.tsx]
autonomous: true
requirements: [EXEC-01, EXEC-02]

must_haves:
  truths:
    - "User can submit code/math in Practice stage and see success/fail feedback"
    - "User completing Quiz triggers stage unlock in the UI"
    - "Apply stage is implemented and allows final project submission"
  artifacts:
    - path: "src/components/stages/ApplyCell.tsx"
      provides: "UI for the final laboratory stage"
  key_links:
    - from: "src/components/stages/*"
      to: "/api/evaluate/*"
      via: "fetch"
---

<objective>
Connect the UI components to the evaluation APIs and finalize the gated learning flow.

Purpose: To provide the end-user with an interactive, feedback-driven learning experience.
Output: Integrated UI cells and a working gated flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-engine-integration/04-02-SUMMARY.md
@src/components/stages/PracticeCell.tsx
@src/components/stages/QuizCell.tsx
@src/components/TopicFlow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Practice and Quiz Cells</name>
  <files>src/components/stages/PracticeCell.tsx, src/components/stages/QuizCell.tsx</files>
  <action>
    1. Update `PracticeCell.tsx`:
       - Implement `handleSubmit` to call `/api/evaluate/practice`.
       - Show success/error toasts or inline alerts.
       - Use `router.refresh()` on success to update the parent `TopicFlow` state.
    2. Update `QuizCell.tsx`:
       - Update `handleNext` (or the final step) to call `/api/evaluate/quiz`.
       - If `passed` is true, show celebratory UI and `router.refresh()`.
  </action>
  <verify>
    <automated>grep "fetch" src/components/stages/PracticeCell.tsx src/components/stages/QuizCell.tsx</automated>
  </verify>
  <done>Practice and Quiz cells are connected to the backend.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Apply Cell</name>
  <files>src/components/stages/ApplyCell.tsx, src/components/TopicFlow.tsx</files>
  <action>
    1. Create `src/components/stages/ApplyCell.tsx`:
       - Implement a "Project Lab" UI.
       - Text area for code submission.
       - "Submit Project" button calling `/api/evaluate/apply`.
       - Final completion state (Big checkmark, "Topic Mastered").
    2. Update `TopicFlow.tsx`:
       - Replace the placeholder in the Apply Block section with the `<ApplyCell />` component.
       - Pass necessary props (topicId, project instruction).
  </action>
  <verify>
    <automated>grep "ApplyCell" src/components/TopicFlow.tsx</automated>
  </verify>
  <done>Apply stage UI is implemented and integrated.</done>
</task>

<task type="auto">
  <name>Task 3: Full Flow Verification</name>
  <files>src/components/TopicFlow.tsx</files>
  <action>
    1. Perform a manual walkthrough (documented in SUMMARY):
       - Complete Practice -> check if Quiz unlocks.
       - Complete Quiz with >80% -> check if Apply unlocks.
       - Complete Apply -> check if Topic is marked as completed.
    2. Fix any state-sync issues between client and server components.
  </action>
  <verify>
    <automated>ls src/components/stages/ApplyCell.tsx</automated>
  </verify>
  <done>Gated learning flow is fully functional and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> API | User input is sent for evaluation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | Quiz Submission | Accept | For the prototype, we assume users won't intercept and forge API calls to unlock stages. |
</threat_model>

<success_criteria>
1. Users can progress through all 5 stages of a topic.
2. Code and math are evaluated in real-time.
3. Successful completion of the final stage marks the topic as completed in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-engine-integration/04-03-SUMMARY.md`
</output>
