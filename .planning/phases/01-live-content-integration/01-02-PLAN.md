---
phase: 01-live-content-integration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/page.tsx, src/app/modules/[id]/page.tsx]
autonomous: true
requirements: [CMS-03, CMS-04]

must_haves:
  truths:
    - "Home page displays all modules from Sanity"
    - "Module detail page displays the correct module based on URL parameter"
    - "Module detail page displays all chapters associated with that module"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Live module listing"
    - path: "src/app/modules/[id]/page.tsx"
      provides: "Live module detail"
---

<objective>
Migrate the home page and module detail pages to use live data from Sanity CMS.

Purpose: Replaces sample data with real learning content.
Output: Dynamic module and chapter listings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/app/modules/[id]/page.tsx
@src/lib/sanity.queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Home page for live modules</name>
  <files>src/app/page.tsx</files>
  <action>
    Convert `Home` to fetch modules using `fetchSanity` and `modulesQuery` from `src/lib/sanity.queries.ts`.
    Map over the fetched modules to render the module cards.
    Ensure appropriate "No modules found" or error states are handled.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Home page renders live modules from Sanity.</done>
</task>

<task type="auto">
  <name>Task 2: Update Module Detail page for live data</name>
  <files>src/app/modules/[id]/page.tsx</files>
  <action>
    Update `ModulePage` to fetch the specific module and its chapters using `moduleByIdQuery`.
    Ensure the `id` from params is passed correctly to the query.
    Update the UI to render the fetched module title, description, and the list of chapters.
    Handle cases where the module is not found (404).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Module detail page renders live content from Sanity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Sanity → UI | Data fetched from Sanity is rendered in components |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | I | Module Detail | mitigate | Ensure Sanity fields are sanitized or handled by React safely (default) |
</threat_model>

<verification>
Check that the Home page and Module detail pages render correctly with Sanity data (mock or real if possible).
</verification>

<success_criteria>
- Home page shows real modules.
- Module detail page shows real chapters.
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-content-integration/01-02-SUMMARY.md`
</output>
