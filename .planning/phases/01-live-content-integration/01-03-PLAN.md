---
phase: 01-live-content-integration
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/app/chapters/[slug]/page.tsx, src/data/sample-content.ts]
autonomous: true
requirements: [CMS-02, CMS-04]

must_haves:
  truths:
    - "Chapter page displays content from Sanity"
    - "Portable Text (including example sentences) renders correctly"
    - "Sample content file is deleted"
  artifacts:
    - path: "src/app/chapters/[slug]/page.tsx"
      provides: "Live chapter content"
---

<objective>
Complete the content integration by migrating chapter pages and removing legacy sample data.

Purpose: Finalizes the transition to a CMS-driven platform.
Output: Dynamic chapter content and clean codebase.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/chapters/[slug]/page.tsx
@src/data/sample-content.ts
@src/lib/sanity.queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Chapter page for live content</name>
  <files>src/app/chapters/[slug]/page.tsx</files>
  <action>
    Update `ChapterPage` to fetch chapter data using `chapterBySlugQuery`.
    Refine the content rendering logic to handle live Sanity `block` and `exampleSentence` types.
    Ensure the "Exit Lesson" and "Next Lesson" links use real data (e.g., module ID and next chapter slug).
    Handle loading and not-found states.
    (Optional) Consider splitting into a Server Component (fetcher) and Client Component (interactive buttons) for better performance.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Chapter page renders live content from Sanity.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup sample data</name>
  <files>src/data/sample-content.ts</files>
  <action>
    Delete `src/data/sample-content.ts`.
    Remove any remaining imports of `sample-content.ts` in the codebase.
    Verify that the build still passes.
  </action>
  <verify>
    <automated>ls src/data/sample-content.ts || echo "Deleted"</automated>
  </verify>
  <done>Sample data is completely removed from the project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Sanity Content → HTML | Portable Text rendering |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | I | Chapter Page | mitigate | Ensure manual Portable Text mapping is secure and handles all expected types |
</threat_model>

<verification>
Check that the Chapter page renders correctly and that no sample data references remain.
</verification>

<success_criteria>
- Chapter content is live.
- Base code is free of sample data.
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-content-integration/01-03-SUMMARY.md`
</output>
