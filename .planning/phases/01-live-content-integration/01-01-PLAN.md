---
phase: 01-live-content-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/sanity.ts, src/lib/sanity.queries.ts, src/middleware.ts]
autonomous: true
requirements: [CMS-01, CMS-04]

must_haves:
  truths:
    - "Sanity client is configured to use environment variables"
    - "GROQ queries for modules and chapters are centralized and typed"
    - "Middleware does not block access to learning routes (per D-PUBLIC-ACCESS)"
  artifacts:
    - path: "src/lib/sanity.queries.ts"
      provides: "GROQ query definitions"
    - path: "src/middleware.ts"
      provides: "Updated routing permissions"
  key_links:
    - from: "src/lib/sanity.queries.ts"
      to: "Sanity CMS"
      via: "GROQ"
---

<objective>
Establish a robust data access layer for Sanity CMS and ensure the platform is accessible to the public as per project decisions.

Purpose: Decouples fetching logic from UI components and enables public access.
Output: Sanity query library and open routing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/sanity.ts
@src/middleware.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define GROQ queries and Sanity helpers</name>
  <files>src/lib/sanity.queries.ts, src/lib/sanity.ts</files>
  <action>
    Create `src/lib/sanity.queries.ts` with the following queries:
    - `modulesQuery`: Fetches all modules ordered by 'order' ascending.
    - `moduleByIdQuery`: Fetches a module by `_id` and includes its chapters.
    - `chapterBySlugQuery`: Fetches a chapter by `slug.current`.
    Update `src/lib/sanity.ts` to include a helper function `fetchSanity<T>(query: string, params?: any)` that uses `client.fetch` with appropriate cache tags (e.g., `{ next: { tags: ['content'] } }`).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Query file exists and exports typed query strings and helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Open public access in middleware</name>
  <files>src/middleware.ts</files>
  <action>
    Modify `src/middleware.ts` to remove protection from `/chapters` and `/modules` routes to align with the D-PUBLIC-ACCESS decision in STATE.md. 
    Either remove the middleware entirely if no other routes need protection, or update the `matcher` to exclude learning routes.
  </action>
  <verify>
    <automated>grep -v "/chapters" src/middleware.ts && grep -v "/modules" src/middleware.ts</automated>
  </verify>
  <done>Middleware no longer intercepts learning routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Sanity → API | Content from CMS is untrusted; must be handled safely during rendering |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | Chapter Rendering | mitigate | Use Sanity's Portable Text patterns to avoid XSS from content |
</threat_model>

<verification>
Check that `src/lib/sanity.queries.ts` is correctly typed and that `src/middleware.ts` allows public access.
</verification>

<success_criteria>
- Sanity client is fully functional.
- Learning routes are accessible without authentication.
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-content-integration/01-01-SUMMARY.md`
</output>
