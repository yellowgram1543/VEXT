---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/prisma.ts, src/app/chapters/[slug]/page.tsx, src/app/modules/[id]/page.tsx]
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Prisma client is successfully generated and can be imported without errors"
    - "Dynamic routes handle params as Promises to maintain compatibility with Next.js 15+"
  artifacts:
    - path: "node_modules/.prisma/client/index.d.ts"
      provides: "Generated Prisma Client"
    - path: "src/lib/prisma.ts"
      provides: "Thread-safe Prisma singleton"
  key_links:
    - from: "src/app/chapters/[slug]/page.tsx"
      to: "React.use(params)"
      via: "Promise unwrapping"
    - from: "src/app/modules/[id]/page.tsx"
      to: "await params"
      via: "Promise unwrapping"
---

<objective>
Fix critical stability issues related to Prisma and Next.js 15+ routing, and generate the Prisma client.

Purpose: Ensure the foundation is solid before implementing authentication features.
Output: Fixed singleton, updated dynamic routes, and generated database client.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/prisma.ts
@src/app/chapters/[slug]/page.tsx
@src/app/modules/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Prisma Client</name>
  <files>prisma/schema.prisma</files>
  <action>
    Run `npx prisma generate` to create the Prisma Client based on the existing schema.
    This is necessary for the imports in `src/lib/prisma.ts` and NextAuth to work correctly.
  </action>
  <verify>
    <automated>npx prisma generate</automated>
  </verify>
  <done>Prisma client generated successfully without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Prisma Singleton Syntax</name>
  <files>src/lib/prisma.ts</files>
  <action>
    Update `src/lib/prisma.ts` to ensure it correctly implements the singleton pattern for Next.js 15+.
    Verify that `globalThis` is used and that there are no type errors with the generated client.
  </action>
  <verify>
    <automated>npm run dev -- --port 3001 --exit-after-start</automated>
  </verify>
  <done>Server starts without Prisma-related initialization errors.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Next.js 15 Params Promise Issues</name>
  <files>src/app/chapters/[slug]/page.tsx, src/app/modules/[id]/page.tsx</files>
  <action>
    Update dynamic route pages to handle `params` as a Promise.
    - In `src/app/chapters/[slug]/page.tsx` (Client Component): Use `React.use(params)` to unwrap the slug.
    - In `src/app/modules/[id]/page.tsx` (Server Component): Change to `async` and `await params`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build passes without "params should be awaited" errors in these files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> App | Prisma Client manages the connection to the PostgreSQL database. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Prisma Client | mitigate | Use generated client with strict types to prevent injection. |
</threat_model>

<success_criteria>
- Prisma client generated.
- `src/lib/prisma.ts` correctly exports a singleton.
- Chapter and Module pages are compatible with Next.js 15+ routing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
