---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [src/components/layout/Navbar.tsx, src/app/layout.tsx]
autonomous: false
requirements: [AUTH-03]
must_haves:
  truths:
    - "Active user's name is visible in the Navbar when signed in"
    - "Navbar is accessible on all pages and adapts to the session state"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Universal header with session awareness"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Navbar.tsx"
      via: "Static import"
---

<objective>
Update the UI to surface session information and perform final phase verification.

Purpose: Provide user feedback on authentication status and finalize the phase.
Output: Navbar with session display and a fully verified Foundation & Auth phase.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navbar Component with Profile Support</name>
  <files>src/components/layout/Navbar.tsx</files>
  <action>
    Create a `Navbar` component that uses `useSession` from `next-auth/react`.
    It should:
    - Display the app name and links to home/modules.
    - Show the authenticated user's name if a session exists.
    - Show a "Sign In" link to `/login` if no session exists.
    Ensure it's styled consistently with the existing UI.
  </action>
  <verify>
    <automated>npm run dev -- --port 3001 --exit-after-start</automated>
  </verify>
  <done>Navbar component created and session hooks used correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Navbar to Root Layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Import the new `Navbar` component into `src/app/layout.tsx` and place it at the top of the body.
    Ensure it's inside the `AuthContext` to correctly access the session.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Navbar successfully integrated into the root layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authentication flow, route protection, and Navbar UI.</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev -- --port 3001`.
    2. Try to access `/modules/m1` - it should redirect you to `/login`.
    3. Visit the home page - you should see the Navbar with a "Sign In" link.
    4. Verify that the Prisma client is correctly working by checking the console logs for any connection errors.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> User | Profile info is displayed only to the authenticated session owner. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | Navbar | mitigate | Use `useSession` to safely access and display public user information. |
| T-01-03-02 | Spoofing | Navbar | mitigate | Ensure session status is managed server-side and client-side via NextAuth. |
		</threat_model>

<success_criteria>
- User can see their name in the Navbar when logged in.
- UI responds correctly to authenticated vs. unauthenticated states.
- Database connection confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
