---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/api/auth/[...nextauth]/route.ts, src/middleware.ts, src/app/login/page.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: "PostgreSQL Database"
    why: "Prisma Adapter stores user data"
    env_vars:
      - name: "DATABASE_URL"
        source: "Connection string from database provider"
  - service: "NextAuth"
    why: "Authentication session management"
    env_vars:
      - name: "NEXTAUTH_SECRET"
        source: "Random string (e.g., `openssl rand -base64 32`)"
      - name: "NEXTAUTH_URL"
        source: "Application base URL (e.g., `http://localhost:3000`)"
must_haves:
  truths:
    - "Unauthenticated users are redirected from protected routes to the login page"
    - "Login page exists and provides the standard NextAuth credentials entry"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Route protection"
    - path: "src/app/login/page.tsx"
      provides: "Sign-in interface"
  key_links:
    - from: "src/middleware.ts"
      to: "/login"
      via: "Automatic redirect"
---

<objective>
Configure NextAuth with the Prisma adapter and implement middleware-based route protection.

Purpose: Secure the learning content and provide a user login experience.
Output: Middleware protecting `/chapters` and `/modules`, functional login page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/auth/[...nextauth]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: NextAuth Prisma Adapter Integration</name>
  <files>src/app/api/auth/[...nextauth]/route.ts</files>
  <action>
    Verify that `NextAuth` is correctly configured with the `PrismaAdapter` in `src/app/api/auth/[...nextauth]/route.ts`.
    Ensure the `PrismaClient` singleton is used.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>NextAuth route is configured without syntax or type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Simple Login Page</name>
  <files>src/app/login/page.tsx</files>
  <action>
    Create a simple login page at `src/app/login/page.tsx` that uses `signIn('credentials')` from `next-auth/react`.
    The UI can be minimal but must be functional enough for redirection testing.
  </action>
  <verify>
    <automated>test -f src/app/login/page.tsx</automated>
  </verify>
  <done>Login page exists and exports a default component.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Middleware Protection</name>
  <files>src/middleware.ts</files>
  <action>
    Create `src/middleware.ts` in the project root (or `src/` directory).
    Use `next-auth/middleware` to protect the following paths:
    - `/chapters/:path*`
    - `/modules/:path*`
    Redirect unauthenticated users to `/login`.
  </action>
  <verify>
    <automated>curl -I http://localhost:3001/chapters/some-slug</automated>
  </verify>
  <done>Middleware correctly handles access control and redirects to login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> App | NextAuth middleware acts as a gate for protected routes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Elevation of Privilege | Middleware | mitigate | Protect sensitive routes using proven `next-auth/middleware`. |
</threat_model>

<success_criteria>
- NextAuth configured with Prisma.
- `/chapters` and `/modules` routes are protected.
- Login page is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
