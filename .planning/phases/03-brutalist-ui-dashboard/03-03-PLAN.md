---
phase: 03-brutalist-ui-dashboard
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [src/app/chapters/[slug]/page.tsx, src/components/TopicStepper.tsx, src/components/stages/UnderstandCell.tsx, src/components/stages/PracticeCell.tsx, src/components/stages/QuizCell.tsx]
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "Topic page features a 5-stage stepper based on the linear_regression_dashboard design"
    - "Stages are locked/unlocked based on the Prisma progress state"
    - "Practice stage includes a code editor UI with brutalist action buttons"
  artifacts:
    - path: "src/components/TopicStepper.tsx"
      provides: "Stage navigation row with locking logic"
    - path: "src/components/stages/PracticeCell.tsx"
      provides: "Notebook-style coding practice interface"
---

<objective>
Implement the "Guided Topic Flow" with stage-specific components and locking logic. This is the heart of the learning experience, where users interact with Sanity content and advance through the gated stages.

Purpose: Enable active, gated learning through a technical, notebook-style interface.
Output: Dynamic topic pages with stage-aware navigation and interactive learning cells.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-brutalist-ui-dashboard/03-RESEARCH.md
@src/app/chapters/[slug]/page.tsx
@src/lib/learning-engine.ts
@stitch_ml_dashboard/practice_block_card_1/code.html
@stitch_ml_dashboard/quiz_block_card_1/code.html
@stitch_ml_dashboard/linear_regression_dashboard/code.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Topic Stepper & Locking Logic</name>
  <files>src/components/TopicStepper.tsx, src/app/chapters/[slug]/page.tsx</files>
  <action>
    Implement the `TopicStepper.tsx` component:
    1. Render 5 stages: Understand (Content), Reinforce (Example), Practice, Test (Quiz), Apply.
    2. Use icons: `check_circle` (completed), `lock` (locked), and stage-specific icons (edit_note, quiz) for active.
    3. Use the `highestStage` from `getTopicProgress` to determine the state of each stage.
    4. Update the Topic Page to use this stepper and conditionally render the active stage component.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Gated stage navigation is functional and respects Prisma state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Understand" Cell (Content/Example)</name>
  <files>src/components/stages/UnderstandCell.tsx</files>
  <action>
    Create `UnderstandCell.tsx`:
    - Render Sanity rich text using the brutalist "Cell" style.
    - Implement the "Socratic Prompt" component (question, hint, explanation) with a 3px border and shadow.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Understand stage content is rendered in a notebook-style cell.</done>
</task>

<task type="auto">
  <name>Task 3: Implement "Practice" Cell (Coding/Math)</name>
  <files>src/components/stages/PracticeCell.tsx</files>
  <action>
    Create `PracticeCell.tsx` based on `practice_block_card_1`:
    - Display the coding prompt and starting code block (JetBrains Mono).
    - Provide a textarea for user input.
    - Implement "Run Cell" and "Submit Task" buttons with the tactile brutalist effect.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Practice stage provides a technical interface for coding exercises.</done>
</task>

<task type="auto">
  <name>Task 4: Implement "Quiz" Cell</name>
  <files>src/components/stages/QuizCell.tsx</files>
  <action>
    Create `QuizCell.tsx` based on `quiz_block_card_1`:
    - Display the question text.
    - Render options as large, brutalist buttons.
    - Implement the submit flow and performance feedback.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Quiz stage allows for assessment within the guided flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Server | Quiz score submission must be verified on the server (Phase 4) |
</threat_model>

<verification>
1. Manually test that clicking a locked stage in the stepper does not navigate or unlock it.
2. Verify that completing a quiz correctly updates the `highestStage` to `APPLY` (simulated if EXEC is not fully ready).
</verification>

<success_criteria>
- Learning flow is strictly gated and sequential.
- UI elements (buttons, inputs) feel physical and responsive.
- Content is presented in a clear, high-density notebook format.
</success_criteria>

<output>
After completion, create `.planning/phases/03-brutalist-ui-dashboard/03-03-SUMMARY.md`
</output>
