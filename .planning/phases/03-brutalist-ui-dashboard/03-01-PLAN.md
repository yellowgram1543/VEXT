---
phase: 03-brutalist-ui-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tailwind.config.ts, src/app/globals.css, src/app/layout.tsx, src/components/layout/TopNav.tsx, src/components/layout/Sidebar.tsx]
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Tailwind config includes custom colors, fonts, and spacing from DESIGN.md"
    - "Global CSS defines the .neo-brutal-shadow and .neo-brutal-interactive classes"
    - "The application uses a fixed TopNav and Sidebar layout"
  artifacts:
    - path: "src/components/layout/TopNav.tsx"
      provides: "Brutalist top navigation bar"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Module-aware left sidebar"
---

<objective>
Implement the core Neo-Brutalist design system and the global layout shell. This establishes the visual foundation and structural navigation for the entire application.

Purpose: Realize the "Technical Playfulness" aesthetic and provide a consistent navigation frame.
Output: Design tokens, utility classes, and a persistent application shell.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-brutalist-ui-dashboard/03-RESEARCH.md
@stitch_ml_dashboard/technical_playfulness/DESIGN.md
@stitch_ml_dashboard/top_navigation_bar/code.html
@stitch_ml_dashboard/left_sidebar_navigation/code.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Design Tokens in Tailwind</name>
  <files>tailwind.config.ts</files>
  Update `tailwind.config.ts` with values from `DESIGN.md`:
  1. Colors: Add `brand-dark: "#330C2F"`, `accent-purple: "#7B287D"`, and the surface/primary/secondary palette.
  2. Fonts: Configure `Space Grotesk` (headings), `Inter` (body), and `JetBrains Mono` (code).
  3. Spacing: Port the 4px base unit and cell-gap.
  4. Border Radius: Add `neo: "10px"` and `neo-sm: "4px"`.
  </action>
  <verify>
  <automated>grep "neo-sm" tailwind.config.ts</automated>
  </verify>
  <done>Tailwind configuration reflects the Neo-Brutalist design system.</done>
  </task>

  <task type="auto">
  <name>Task 2: Define Global Brutalist Utilities</name>
  ...
  <done>Application shell with TopNav and Sidebar is functional.</done>
  </task>

  <task type="auto">
  <name>Task 4: Align Backend Stage Types (Understand, Reinforce, Practice, Test, Apply)</name>
  <files>prisma/schema.prisma, src/lib/learning-engine.ts</files>
  <action>
  Update the backend to support the 5-stage UI flow:
  1. Add `PRACTICE` to the `StageType` enum in `prisma/schema.prisma`.
  2. Run `npx prisma db push && npx prisma generate`.
  3. Update `src/lib/learning-engine.ts`:
     - Add `StageType.PRACTICE` to `STAGE_ORDER` between `REINFORCE` and `TEST`.
     - Verify `getNextStage` logic correctly handles the new sequence.
  </action>
  <verify>
  <automated>npx vitest tests/engine.test.ts --run</automated>
  </verify>
  <done>Prisma schema and Learning Engine aligned with the 5-stage UI flow.</done>
  </task>
    Add the following custom utilities to `src/app/globals.css`:
    1. `.neo-brutal-shadow`: `box-shadow: 4px 4px 0px 0px #330C2F;`.
    2. `.neo-brutal-shadow-sm`: `box-shadow: 2px 2px 0px 0px #330C2F;`.
    3. `.neo-brutal-interactive:active`: `transform: translate(4px, 4px); box-shadow: none;`.
    4. Ensure standard borders are 3px solid.
  </action>
  <verify>
    <automated>grep "neo-brutal-shadow" src/app/globals.css</automated>
  </verify>
  <done>Custom CSS utilities for shadows and interactions are defined.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Persistent Layout Shell</name>
  <files>src/app/layout.tsx, src/components/layout/TopNav.tsx, src/components/layout/Sidebar.tsx</files>
  <action>
    Create the application shell based on the stitch assets:
    1. Create `TopNav.tsx`: Implement the 80px fixed bar with logo and navigation tabs.
    2. Create `Sidebar.tsx`: Implement the 256px fixed left sidebar.
    3. Update `src/app/layout.tsx`: 
       - Import and wrap `{children}` with the Nav and Sidebar.
       - Apply the `bg-background` and `font-body-md` classes to the body.
       - Ensure a content area with a `max-w-[1000px]` and `ml-64` (sidebar offset).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Application shell with TopNav and Sidebar is functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → UI | Layout must be responsive to prevent content overlap |
</threat_model>

<verification>
1. Run `npm run dev` and visually verify the fixed sidebar and top nav.
2. Check that the "Space Grotesk" font is loaded for headers.
3. Test the "neo-brutal-interactive" button effect in the browser.
</verification>

<success_criteria>
- Custom colors and fonts are active.
- TopNav and Sidebar are correctly positioned and styled.
- Interactive elements show the characteristic "press" effect.
</success_criteria>

<output>
After completion, create `.planning/phases/03-brutalist-ui-dashboard/03-01-SUMMARY.md`
</output>
