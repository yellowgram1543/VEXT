---
phase: 03-brutalist-ui-dashboard
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/app/page.tsx, src/app/modules/[id]/page.tsx, src/components/ModuleCard.tsx, src/components/ProgressBar.tsx]
autonomous: true
requirements: [UI-01, UI-03]

must_haves:
  truths:
    - "Module cards on the Home page use the brutalist style (3px borders, hard shadows)"
    - "Module Detail page shows a streak counter and overall progress bar"
    - "Progress indicators reflect the actual database state from Prisma"
  artifacts:
    - path: "src/components/ModuleCard.tsx"
      provides: "Neo-Brutalist card component for dashboard"
    - path: "src/components/ProgressBar.tsx"
      provides: "Consistent brutalist progress bar"
---

<objective>
Implement the User Dashboard and Module Detail pages using the Neo-Brutalist design language. This phase focuses on presenting progress and curriculum in a high-signal, engaging format.

Purpose: Provide a clear "Serious Play" dashboard for users to track their ML mastery.
Output: Brutalist-styled Home page, Module cards, and progress visualization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-brutalist-ui-dashboard/03-RESEARCH.md
@src/app/page.tsx
@src/app/modules/[id]/page.tsx
@stitch_ml_dashboard/practice_block_card_1/code.html
@stitch_ml_dashboard/linear_regression_dashboard/code.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Brutalist ModuleCard Component</name>
  <files>src/components/ModuleCard.tsx</files>
  <action>
    Implement a new `ModuleCard.tsx` component based on the dashboard design:
    - 3px border, 10px radius, hard shadow.
    - Large "Space Grotesk" title.
    - Integration with `ProgressBar`.
    - "Active Shift" effect on hover/click.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ModuleCard component implemented with brutalist styling.</done>
</task>

<task type="auto">
  <name>Task 2: Update Home Page (Dashboard)</name>
  <files>src/app/page.tsx</files>
  <action>
    Refactor `src/app/page.tsx`:
    1. Update the header to match the `linear_regression_dashboard` header style.
    2. Replace the old module links with the new `ModuleCard` component.
    3. Ensure real progress data is passed to each card.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Home page refactored into a Neo-Brutalist dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Module Detail Page</name>
  <files>src/app/modules/[id]/page.tsx, src/components/ProgressBar.tsx</files>
  <action>
    Refactor `src/app/modules/[id]/page.tsx`:
    1. Implement the "Current Topic" header from the dashboard asset.
    2. Add the streak counter mockup (7 Day Streak).
    3. Update the `ProgressBar` to match the design (thick borders, sharp vertical fill).
    List chapters as "Notebook Cells" (vertical stack with cell-gap and 3px borders).
    </action>
    <verify>
    <automated>npm run build</automated>
    </verify>
    <done>Module detail page matches the high-signal dashboard design.</done>
    </task>

    <task type="auto">
    <name>Task 4: Implement Skill Mastery Map Visualization</name>
    <files>src/components/SkillMasteryMap.tsx, src/app/page.tsx</files>
    <action>
    Implement the mastery map visualization per UI-03:
    1. Create `SkillMasteryMap.tsx`:
       - Fetch `userMastery` data from Prisma.
       - Render a brutalist-style chart or grid showing performance in Math, Coding, and Concepts.
       - Use the `accent-purple` and `primary` colors for the data points.
    2. Integrate this component into the Home page sidebar or header area.
    </action>
    <verify>
    <automated>npm run build</automated>
    </verify>
    <done>Skill mastery maps provide visual feedback on learning progress.</done>
    </task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CMS/DB → UI | Rendering untrusted text strings in headers |
</threat_model>

<verification>
1. Verify that progress percentages on the home page match the module detail pages.
2. Confirm that the "Mastered" checkmarks appear correctly in the curriculum list.
</verification>

<success_criteria>
- Dashboard feels authoritative and kinetic.
- Progress bars are visually consistent and accurate.
- Module detail page follows the "Cell" layout model.
</success_criteria>

<output>
After completion, create `.planning/phases/03-brutalist-ui-dashboard/03-02-SUMMARY.md`
</output>
