---
phase: 01-state-machine-schema-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, package.json, vitest.config.ts, tests/schema.test.ts]
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04]

must_haves:
  truths:
    - "Prisma schema includes Phase -> Module -> Topic hierarchy (STATE-01)"
    - "highestStage Enum includes UNDERSTAND, REINFORCE, TEST, APPLY (STATE-02)"
    - "TopicProgress model tracks user progress per topic (STATE-02)"
    - "QuizAttempt and UserMastery models are defined for performance tracking (STATE-03, STATE-04)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Source of truth for learning engine state"
    - path: "tests/schema.test.ts"
      provides: "Automated verification of schema relations"
  key_links:
    - from: "User"
      to: "TopicProgress"
      via: "userId"
    - from: "Topic"
      to: "TopicProgress"
      via: "topicId"
---

<objective>
Update the Prisma schema to implement the ML Cognitive Coach gated learning engine foundation and setup the testing infrastructure.

Purpose: Decouple state from content and establish the rules for the sequential stage-locking system with automated validation.
Output: Updated schema, migration, generated client, and Vitest-based schema verification suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-state-machine-schema-engine/RESEARCH.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Testing Infrastructure</name>
  <files>package.json, vitest.config.ts, tests/schema.test.ts</files>
  <action>
    Install Vitest and setup the configuration for integration testing:
    1. Install `vitest` as a devDependency.
    2. Create `vitest.config.ts` in the root.
    3. Create a skeleton `tests/schema.test.ts` that imports PrismaClient.
  </action>
  <verify>
    <automated>npm list vitest && test -f vitest.config.ts</automated>
  </verify>
  <done>Vitest is installed and configured for the project.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Learning Engine Schema</name>
  <files>prisma/schema.prisma</files>
  <action>
    Update prisma/schema.prisma to include the 3-tier curriculum hierarchy and state models:
    - Define `enum StageType` with values: UNDERSTAND, REINFORCE, TEST, APPLY (per STATE-02).
    - Create `Phase`, `Module`, and `Topic` models with appropriate relations and @default(cuid()) IDs (STATE-01).
    - Implement `TopicProgress` with `highestStage` Enum (default UNDERSTAND) and unique [userId, topicId] constraint (STATE-02).
    - Implement `QuizAttempt` with score (Float) and passed (Boolean) fields (STATE-03).
    - Implement `UserMastery` for aggregate metrics (STATE-04).
    - Remove the legacy `Progress` model.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Core hierarchy, state enums, and tracking models defined in schema.</done>
</task>

<task type="auto">
  <name>Task 3: Database Synchronization & Integration Test</name>
  <files>prisma/schema.prisma, tests/schema.test.ts</files>
  <action>
    Synchronize the database and verify the schema with automated tests:
    1. Run `npx prisma migrate dev --name init_ml_engine`.
    2. Run `npx prisma generate`.
    3. Implement `tests/schema.test.ts` to verify:
       - Phase -> Module -> Topic relational integrity.
       - TopicProgress unique constraint enforcement.
       - Successful creation of QuizAttempt and UserMastery records.
    4. Execute tests using `npx vitest tests/schema.test.ts --run`.
  </action>
  <verify>
    <automated>npx vitest tests/schema.test.ts --run</automated>
  </verify>
  <done>Database migrated, Prisma Client generated, and schema relations verified via automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Prisma | All state updates must be validated via Prisma Client to enforce Enum constraints. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | TopicProgress | mitigate | Use Prisma Enums and Zod validation in API (Phase 4). |
| T-01-02 | Information Disclosure | UserMastery | mitigate | Ensure userId is indexed and queried strictly via session context. |
</threat_model>

<verification>
1. Run `npx prisma validate` to ensure no schema errors.
2. Run `npx vitest tests/schema.test.ts --run` to confirm relational integrity.
</verification>

<success_criteria>
- Schema supports the Phase -> Module -> Topic hierarchy.
- TopicProgress tracks stage unlocking with UNDERSTAND, REINFORCE, TEST, APPLY enums.
- Database is successfully migrated and verified with automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-machine-schema-engine/01-01-SUMMARY.md`
</output>
