---
phase: 01-state-machine-schema-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/lib/learning-engine.ts, tests/engine.test.ts]
autonomous: true
requirements: [STATE-02, STATE-03]

must_haves:
  truths:
    - "Learning engine logic correctly determines stage access based on Enum indices (STATE-02)"
    - "Sequential unlocking logic is verified for 80% gate transition (STATE-03)"
    - "80% mastery gate is strictly enforced for unlocking 'APPLY' stage (STATE-03)"
  artifacts:
    - path: "src/lib/learning-engine.ts"
      provides: "State machine transition logic"
    - path: "tests/engine.test.ts"
      provides: "Unit tests for stage-locking logic"
---

<objective>
Implement and verify the core state machine logic for the learning engine.

Purpose: Ensure the logic for sequential stage unlocking and mastery gates is sound before building the UI.
Output: A utility library for stage-locking logic and a comprehensive Vitest suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-state-machine-schema-engine/01-01-PLAN.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Learning Engine Logic</name>
  <files>src/lib/learning-engine.ts</files>
  <action>
    Implement pure utility functions for the learning state machine:
    - `isStageUnlocked(highestUnlocked: StageType, requested: StageType): boolean`: Uses index-based comparison of the StageType enum (UNDERSTAND=0, REINFORCE=1, TEST=2, APPLY=3) to verify access.
    - `getNextStage(current: StageType, quizScore?: number): StageType`: Logic to determine the next stage.
    - Enforce the "80% Mastery Gate" (STATE-03): If `current` is `TEST`, only return `APPLY` if `quizScore >= 0.8`. Otherwise, return `TEST` or `REINFORCE` based on requirements.
    
    Ensure `StageType` is imported from `@prisma/client`.
  </action>
  <verify>
    <automated>test -f src/lib/learning-engine.ts</automated>
  </verify>
  <done>Core state machine logic implemented as pure functions.</done>
</task>

<task type="auto">
  <name>Task 2: Engine Logic Verification</name>
  <files>tests/engine.test.ts</files>
  <action>
    Create a Vitest suite to verify the unlocking logic:
    - Test `isStageUnlocked` for all valid/invalid transitions.
    - Test `getNextStage` specifically for the 80% threshold (e.g., 0.79 fails to unlock APPLY, 0.80 succeeds).
    - Test edge cases (first stage, already at last stage).
    
    Execute tests using `npx vitest tests/engine.test.ts --run`.
  </action>
  <verify>
    <automated>npx vitest tests/engine.test.ts --run</automated>
  </verify>
  <done>Learning engine logic verified with 100% test coverage for critical paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Logic in `learning-engine.ts` must be used on the server to prevent client-side state manipulation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Stage Lock Logic | mitigate | Core logic is centralized in `learning-engine.ts` for server-side enforcement. |
</threat_model>

<verification>
1. Run `npx vitest tests/engine.test.ts --run` and confirm all tests pass.
</verification>

<success_criteria>
- State machine logic correctly handles sequential unlocking based on UNDERSTAND -> REINFORCE -> TEST -> APPLY order.
- The 80% mastery gate transition (TEST -> APPLY) is strictly verified by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-machine-schema-engine/01-02-SUMMARY.md`
</output>
