# Graphify Integration
Before using `grep_search`, `read_file`, or `glob`, check `graphify-out/GRAPH_REPORT.md` if it exists. 
It contains the codebase structure and "God Nodes" which will help you navigate more efficiently.
